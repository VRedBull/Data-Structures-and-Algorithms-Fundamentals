#include<iostream>
using namespace std;

int main(){
    // char name = 'A';
    // // printf("%c",name);
    // char name = 65;
    // cout<<name;
    // char X[10];
    // cout<<"Enter: "<<flush;
    // // scanf("%s",X);
    // gets(X);            //gets strings with spaces in between
    // printf("%s",X);
}